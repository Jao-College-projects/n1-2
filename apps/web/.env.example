# Supabase (auth, depoimentos, seções — opcional conforme seu ambiente)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# API Java (Tomcat) — catálogo de produtos + checkout transacional
# Ex.: Docker compose deste repo → http://localhost:8082/luar-api
VITE_API_BASE_URL=http://localhost:8082/luar-api
